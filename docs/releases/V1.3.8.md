# v1.3.8 — New marketplace connection setup

Initialize inventory_activated_at on connection insertion. Connections remain PENDING with no stored authorization. Reconnection preserves the original inventory boundary. No existing rows, publishing configuration, or credentials are changed. Database regression coverage includes Amazon US, Walmart US, and reconnection.
