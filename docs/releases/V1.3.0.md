# v1.3.0 — shared inventory release

## User-facing changes

- Consistent operational table, picture-editing, mapping, collaboration and action controls.
- Account-scoped tagged smart filters, dynamic dates and compact filtered totals.
- Shared catalogue-item inventory fan-out to mapped FBM SKUs, multipacks and bundles.
- Durable publication queue, priority zero updates, verification/retries and a sync status page.
- Individual unresolved-order checks without a purchase-age cutoff. A missing order is not
  a cancellation. Overdue/missing checks retain reservations and expose review warnings.
- Confirmed cancellation releases are logged without adding physical stock. Stale responses
  cannot overwrite a newer cancellation. Packed stock is not automatically returned.
- Partial shipments post only newly shipped components and keep the remainder reserved.
  Missing prior shipment allocations hold all related SKUs at zero with a ledger explanation;
  counting every remaining location/expiration batch releases the hold without retrospective deductions.

## Migration and release status

Migrations 74–78 add smart filters, shared picture overrides, the inventory publication outbox,
unresolved-order status checks and partial-shipment reviews. No local business data or credentials
are copied to production.

The complete isolated local Java build passed, including database regressions. GitHub CI and
the production workflow both passed the full Java suite and all 41 JavaScript/browser checks
on commit `7b07fbb5a30984ef98a7e618a830b9bab0eb6daa`.

Production deployment [35814787840](https://github.com/barryguze-code/next-ai-commerce/actions/runs/35814787840)
succeeded on September 22, 2026 (Pacific). The live application reports v1.3.0, responds to the
login health check with HTTP 200, and has successful migrations through version 78.
The pre-release database dump, prior application and runtime configuration are retained in
`/opt/next-ai-commerce/backups/v1.3.0-7b07fbb5a309-Mg0nis` on the production server.

The partial-shipment fix is included with database regressions covering repeated partial
shipments, packs/bundles, partial cancellation, missing stock history and complete versus
incomplete physical counts. Direct status checks cannot release reservations if Amazon's
shipped-item total differs from the imported item details; those checks retry for review.

Deployment and inventory publishing were authorized on September 22, 2026, specifically for
**The Keto Storehouse / Ibcore LLC / Amazon US**, connection
`6c4d4dae-d69c-4794-91bd-b3173fa56c52`. KARACA and Walmart are excluded.
Live startup requires a nonempty connection allowlist. An optional SKU restriction supports
the first zero/positive verification before expanding within that same approved connection.
Authorization does not replace green CI, backup, health and Amazon read-back gates.

The deployment workflow retains its existing release-branch trigger and all test gates.
Inventory activation is staged separately: quantity preview, restricted two-SKU live test,
then expansion only after Amazon read-back matches both the zero and positive quantities.

Both live checks passed: Amazon read-back matched the zero and positive test quantities.
The zero took several accepted submissions and
processing time; acceptance was never treated as confirmation. A later verification-only
check confirmed the zero without issuing another quantity update.

Publishing was then expanded to the approved connection's eligible mapped FBM SKUs.
The initial queue drains gradually; this is not a claim that every SKU has already been
confirmed by Amazon. FBA, unknown fulfillment channels and never-mapped SKUs are excluded.
The initial activation backup is `inventory-v1.3.0-Udqbt1`; the verified canary configuration
is retained in `rollout-v1.3.0-zDxdkb`, both under the server's backup directory.
KARACA and Walmart remain excluded, the legacy listing worker remains disabled, and
`AMAZON_BUY_SHIPPING_ENABLED=false` remains explicit in the live process configuration.

## Rollout safeguards

1. Confirm the authorized seller/account/marketplace scope and current production revision.
2. Obtain green Java and browser CI on the exact commit; review migration checks and backup.
3. Merge the tested development head into the production release branch (never bypass CI).
4. Verify the server-side backup and health checks performed by the existing deployment.
5. Enable unresolved-order checks and the inventory publisher explicitly in production,
   with legacy listing inventory actions disabled. `AMAZON_BUY_SHIPPING_ENABLED` defaults
   to false and must remain false: label purchasing is not part of this rollout.
   Local Amazon writes remain disabled.
6. Wait for a successful post-startup order reconciliation. The publisher refuses to send
   without a fresh completed order window or while order-import jobs remain unfinished.
7. Verify a small known set of zero/positive quantity submissions against Amazon before
   expanding to all approved mapped FBM SKUs. Never infer confirmation from ACCEPTED alone.
8. On failure, disable publishing first; restore the application/configuration using the
   retained rollback artifact. Application rollback does not undo quantities already sent
   to Amazon or revert database migrations. Reconcile affected SKUs explicitly.

The tested development head was fast-forwarded into `release/v1.0.0` (the existing production
branch name). Hotfix work continues on `development/post-v1.3.0` from that same release base.
