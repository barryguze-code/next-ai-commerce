# Version 0.6.3 — Orders schema alignment

Released: 2026-08-30

- Corrects the Orders marketplace-time query from the nonexistent `marketplace_id` column to the
  established tenant connection field, `marketplace_identifier`.
- Restores Orders page loading while retaining marketplace-local day grouping.
- Adds a schema contract regression test so the lookup cannot silently drift again.
- Changes no marketplace, order, catalogue, receiving, or inventory data.
