# v1.3.7 — Receiving case-size defaults

First receipt retains invoice quantity prefill. Additional receiving starts with zero cases and retains the catalogue units per case, with expiration focused. A compact catalogue-update button replaces the explanatory text; the confirmation remains. Exception clearing still resets both fields. No inventory or price publishing configuration changes.
